namespace nilnul.dev.src.div_
{
	/// <summary>
	/// 
	/// </summary>
	/// alias:
	///		Dist
	///			like disk, a unit to be distributed.
	///		Distro
	///			distritable as module top.
	///		
	///		modulable
	///			can be initiated to be a git module. but the cognomen of this can be empty
	///			confusing with nilnul.fs.folder_.Modulabe,which is a folder where a module can be initiated. That is: it's a folder that is nonrepo
	/// 
	interface IDist {

	}
}
